---
  - name: Ansible Playbook
    hosts: all
    become: true
    tasks:
      - name: Update cache 
        apt:
          update_cache: yes
      - name: install some plugin
        apt: 
         name: ['apt-transport-https', 'ca-certificates']
         state: absent
         update_cache: yes
        
        


      